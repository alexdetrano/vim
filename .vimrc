execute pathogen#infect()
syntax enable  " enable syntax highlighting
set tabstop=2  " number of visual spaces per TAB
set softtabstop=2  "number of spaces to insert when TAB is pressed"
set expandtab  "pressing TAB in insert mode produces spaces
set shiftwidth=2  " number of columns to indent
set number  " show line numbers
" set cursorline  " highlight current line
filetype indent on  " load filetype-specific indent files
set showmatch  " highlight matching [{()}]
set incsearch  " search as characters are entered
set hlsearch  " highlight matches
set mouse=a  " don't scroll terminal with mouse, scroll only vim (iterm2 fix) 

